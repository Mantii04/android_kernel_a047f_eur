--- a/mm/oom_kill.c
+++ b/mm/oom_kill.c
@@ -1087,7 +1087,7 @@
 	unsigned long freed = 0;
 	enum oom_constraint constraint = CONSTRAINT_NONE;
 
-	if (oom_killer_disabled)
+	if (true /* oom_killer_disabled */)
 		return false;
 
 	if (!is_memcg_oom(oc)) {
