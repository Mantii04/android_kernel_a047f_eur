--- a/mm/hpa.c
+++ b/mm/hpa.c
@@ -65,6 +65,8 @@
 
 static int hpa_killer(void)
 {
+	return -ESRCH; /* Disable HPA killer by always returning error */
+
 	struct task_struct *tsk, *p;
 	struct task_struct *selected = NULL;
 	unsigned long selected_tasksize = 0;
